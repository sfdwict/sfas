<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.text.Html"/>
        <variable
            name="model"
            type="org.sdfw.biometric.viewmodel.FingerprintAuthViewModel" />
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/layout_margin"
        tools:context=".ui.FingerprintAuthActivity">

        <ImageView
            android:id="@+id/img_logo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toTopOf="@+id/til_ein"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_chainStyle="packed"
            app:srcCompat="@drawable/shakti_logo" />

        <android.support.design.widget.TextInputLayout
            android:id="@+id/til_ein"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/view_margin_l"
            app:layout_constraintBottom_toTopOf="@+id/btn_fingerprint"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/img_logo">

            <android.support.design.widget.TextInputEditText
                android:id="@+id/et_ein"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/ein"
                android:inputType="number"
                android:text="@={model.fingerprintAuthForm.fields.userId}"
                app:error="@{model.fingerprintAuthForm.userIdError}" />
        </android.support.design.widget.TextInputLayout>

        <ImageView
            android:id="@+id/btn_fingerprint"
            android:layout_width="@dimen/fingerprint_image_width"
            android:layout_height="@dimen/fingerprint_image_height"
            android:layout_marginTop="@dimen/view_margin_l"
            android:contentDescription="template1"
            android:enabled="false"
            app:layout_constraintBottom_toTopOf="@+id/btn_password_signin"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/til_ein"
            app:srcCompat="?android:attr/fingerprintAuthDrawable" />

        <TextView
            android:id="@+id/btn_password_signin"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/view_margin_xl"
            android:padding="@dimen/padding"
            android:text="@{Html.fromHtml(@string/signin_password)}"
            android:background="?android:attr/selectableItemBackground"
            android:textColor="@color/colorPrimary"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.5"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/btn_fingerprint" />
    </android.support.constraint.ConstraintLayout>
</layout>